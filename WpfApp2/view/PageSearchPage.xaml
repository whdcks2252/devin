<Page x:Class="WpfApp2.view.PageSearchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp2.view"
      mc:Ignorable="d"
      Title="PageSearchPage" Height="97.34" Width="488">

    <Grid>
        <TextBox Text="{Binding SeachTextBoxTx, UpdateSourceTrigger=PropertyChanged}"  VerticalAlignment="Top" TextAlignment="Center" Margin="337,26,67,0" Height="30" Width="83.9999999999999"></TextBox>
        <Button Command="{Binding DownPage}" Margin="86,22,361,43" FontSize="20">&lt;</Button>
        <Button Command="{Binding UpPage}"  Margin="182,22,264,44" FontSize="20">&gt;</Button>
        <TextBlock Text="페이지 검색 :" FontSize="17" Margin="229,27,144,43"/>
        <TextBlock Text="{Binding MaxAndCurPage , UpdateSourceTrigger=PropertyChanged}" TextAlignment="Center" FontSize="13"  Margin="137,27,315,22"></TextBlock>
        <Button FontSize="15" Command="{Binding FindPage}" Margin="430,25,7,42" Width="50.9999999999999" Height="30.34">검색</Button>
    </Grid>
</Page>
